# Gold Scout Global V6.4

Manual-refresh stable heat map.

Main fix:
- Heat map no longer refreshes while panning or zooming.
- Move map first, then tap Refresh Heat Map.
- Heat map stays fixed until you manually refresh or clear it.
- Includes river/creek data from OpenStreetMap when available.
- Falls back to a selected-point heat map if no mapped waterway is found.
